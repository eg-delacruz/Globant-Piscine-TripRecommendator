FROM node:20-alpine

WORKDIR /app

# Install dependencies globally for convenience
RUN npm install -g npm@latest

# Expose Vite's default port
EXPOSE 5173

# Keep container running and allow interactive commands
CMD ["sh"]